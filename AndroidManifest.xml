<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tzapps.tzpalette"
    android:versionCode="1"
    android:versionName="0.0.1" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />
    
    <uses-feature
        android:name="android.hardware.camera"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/CustomActionBarTheme" >
        
        <!-- The main activity -->
        <activity
            android:name="com.tzapps.tzpalette.activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- The child activity to display the picture -->
        <activity
            android:name="com.tzapps.tzpalette.activity.ShowPictureActivity"
            android:label="@string/title_activity_show_picture"
            android:parentActivityName="com.tzapps.tzpalette.activity.MainActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.tzapps.tzpalette.activity.MainActivity" />
        </activity>
        
    </application>

</manifest>